#ifndef LIL_WEB_ENVIRONMENT_INCLUDED
#define LIL_WEB_ENVIRONMENT_INCLUDED

// Three.js PMREM and Unity reflection probes are not equivalent. The initial
// WebGL ABI exposes a single cube texture and lets upstream lilToon perform its
// normal reflection blend.

#endif
