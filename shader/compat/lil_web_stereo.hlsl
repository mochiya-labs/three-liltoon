#ifndef LIL_WEB_STEREO_INCLUDED
#define LIL_WEB_STEREO_INCLUDED

// WebXR renders once per eye. The renderer adapter refreshes the camera ABI for
// the active eye, so Unity single-pass stereo declarations remain disabled.

#endif
